import { Breadcrumb, Title } from "../../components/Components";

const Components = () => {
  return (
    <div>
      <Title title="components" />
      <Breadcrumb />
    </div>
  );
};

export default Components;
