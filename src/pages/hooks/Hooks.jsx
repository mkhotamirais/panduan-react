import { Breadcrumb, Title } from "../../components/Components";

const Hooks = () => {
  return (
    <div>
      <Title title="hooks" />
      <Breadcrumb />
    </div>
  );
};

export default Hooks;
